@export(child)
contract child { }

@test(import_child)
import "child"
contract parent { }

@export(grand_child)
contract grand_child { }

@export(child)
import "grand_child"
contract child { }

@test(import_grand_child)
import "child"
contract test { }

@export(sub)
import "main"
contract sub { }

@export(main)
import "sub"
contract main { }

@test(cross_reference)
@error(ERROR_CROSS_IMPORT)
import "main"
contract test { }
