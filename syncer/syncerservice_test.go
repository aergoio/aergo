package syncer

import (
	//			"github.com/aergoio/aergo/types"
	"testing"
)

func TestSyncer_sync100(t *testing.T) {
	//init test ----------------------------------
	/*
		testTargetNo := uint64(100)


		//make remoteBlockChain
		remoteChain := initStubBlockChain(110)

		ancestor := remoteChain.GetBlockInfo(0)

		//SetTestEnvironment
		setTest := func (peers []StubPeer, targetNo int, ancestor *types.BlockInfo) {
			ctx := types.NewSyncCtx("p1", testTargetNo, 1)
			ctx.SetAncestor(ancestor)
		}

		syncer := NewStubSyncer(ctx, true, true, remoteChain)

		syncer.start()
	*/
}
